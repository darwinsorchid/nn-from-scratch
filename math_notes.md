## Formulas

### ReLU (forward propagation)

$$
f(x) = max(1,x)
$$

### Sigmoid (forward propagation)

$$
f(x) = \frac{1}{1 + e^{-x}}
$$

### Binary Cross-Entropy (BCE) Loss Formula

$$
\mathrm{BCE} = -\frac{1}{n} \sum_{i=1}^{n} [y\log_{\hat{y}} + (1 - y)\log_{(1- \hat{y})}]
$$
