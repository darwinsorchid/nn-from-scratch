# NumPy NN vs Keras NN

Both models have the same architecture, learning rate and loss function.

## 1. Network Initialization

Initialize layers and parameter (weight + bias) values per layer.
<br>

## 2. Define activation functions

## 3. Loss Function

Binary classification -> Binary Cross-Entropy (BCE) Loss

# Run

```bash
python main.py
```
